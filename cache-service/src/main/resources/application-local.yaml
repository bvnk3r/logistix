server:
  port: 8201

spring:
  application:
    name: @project.artifactId@
  config:
    import: optional:configserver:http://localhost:8071
  mvc:
    log-request-details: true
  security:
    enabled: false